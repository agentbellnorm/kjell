## Cross-validation against safecmd (https://github.com/AnswerDotAI/safecmd)
## Every command safecmd allows should be read; every command it blocks should be write/unknown.

## === safecmd ALLOWED commands ===

[[tests]]
input = "ls -la"
expect = "safe"

[[tests]]
input = "ls -la | grep py"
expect = "safe"

[[tests]]
input = "git status"
expect = "safe"

[[tests]]
input = "find . -name '*.py'"
expect = "safe"

[[tests]]
input = "echo hello"
expect = "safe"

[[tests]]
input = "find . -exec ls \\;"
expect = "safe"

[[tests]]
input = "env ls"
expect = "safe"

[[tests]]
input = "sed 1,3p styles.css"
expect = "safe"

[[tests]]
input = "echo hello | cat"
expect = "safe"

[[tests]]
input = "[ -f /etc/passwd ] && echo exists"
expect = "safe"

[[tests]]
input = "git status && echo done"
expect = "safe"

[[tests]]
input = "cat file | grep error"
expect = "safe"

[[tests]]
input = "head -20 file.txt"
expect = "safe"

[[tests]]
input = "tail -f /var/log/syslog"
expect = "safe"

[[tests]]
input = "wc -l file.txt"
expect = "safe"

[[tests]]
input = "sort file.txt"
expect = "safe"

[[tests]]
input = "uniq -c"
expect = "safe"

[[tests]]
input = "diff file1 file2"
expect = "safe"

[[tests]]
input = "cut -d: -f1 /etc/passwd"
expect = "safe"

[[tests]]
input = "tr '[:lower:]' '[:upper:]'"
expect = "safe"

[[tests]]
input = "file myfile.bin"
expect = "safe"

[[tests]]
input = "stat file.txt"
expect = "safe"

[[tests]]
input = "du -sh ."
expect = "safe"

[[tests]]
input = "df -h"
expect = "safe"

[[tests]]
input = "which python"
expect = "safe"

[[tests]]
input = "whereis ls"
expect = "safe"

[[tests]]
input = "whoami"
expect = "safe"

[[tests]]
input = "hostname"
expect = "safe"

[[tests]]
input = "uname -a"
expect = "safe"

[[tests]]
input = "date"
expect = "safe"

[[tests]]
input = "uptime"
expect = "safe"

[[tests]]
input = "printenv PATH"
expect = "safe"

[[tests]]
input = "basename /path/to/file.txt"
expect = "safe"

[[tests]]
input = "dirname /path/to/file.txt"
expect = "safe"

[[tests]]
input = "realpath ./relative/path"
expect = "safe"

[[tests]]
input = "seq 1 10"
expect = "safe"

[[tests]]
input = "cal"
expect = "safe"

[[tests]]
input = "tree ."
expect = "safe"

[[tests]]
input = "rg TODO src/"
expect = "safe"

[[tests]]
input = "ag pattern file"
expect = "safe"

[[tests]]
input = "ack pattern"
expect = "safe"

[[tests]]
input = "fgrep literal file.txt"
expect = "safe"

[[tests]]
input = "egrep 'pattern1|pattern2' file.txt"
expect = "safe"

[[tests]]
input = "jq '.data' file.json"
expect = "safe"

## Git read operations

[[tests]]
input = "git log --oneline -20"
expect = "safe"

[[tests]]
input = "git diff HEAD~3"
expect = "safe"

[[tests]]
input = "git branch -a"
expect = "safe"

[[tests]]
input = "git show HEAD"
expect = "safe"

[[tests]]
input = "git remote -v"
expect = "safe"

[[tests]]
input = "git tag -l"
expect = "safe"

[[tests]]
input = "git blame file.txt"
expect = "safe"

## gh read operations

[[tests]]
input = "gh pr list"
expect = "safe"

[[tests]]
input = "gh issue view 123"
expect = "safe"

[[tests]]
input = "gh run list"
expect = "safe"

## npm/package manager reads

[[tests]]
input = "npm list"
expect = "safe"

[[tests]]
input = "npm ls --depth 0"
expect = "safe"

[[tests]]
input = "npm test"
expect = "safe"

## Docker reads

[[tests]]
input = "docker ps"
expect = "safe"

[[tests]]
input = "docker images"
expect = "safe"

[[tests]]
input = "docker logs my-container"
expect = "safe"

[[tests]]
input = "docker inspect my-container"
expect = "safe"

## === safecmd BLOCKED commands ===

[[tests]]
input = "rm -rf /"
expect = "write"

[[tests]]
input = "find / -exec rm {} \\;"
expect = "write"
note = "find -exec with destructive inner command"

[[tests]]
input = "find . -delete"
expect = "write"

[[tests]]
input = "git push"
expect = "write"

[[tests]]
input = "sudo ls"
expect = "safe"
note = "We classify sudo ls as read (recursive eval). safecmd blocks all sudo. Our approach is more permissive but correct."

[[tests]]
input = "env rm -rf /asdfff"
expect = "write"

[[tests]]
input = "ls $(rm -rf /)"
expect = "write"
note = "Destructive command substitution"

[[tests]]
input = "xargs rm"
expect = "write"

## === Write operations safecmd blocks ===

[[tests]]
input = "npm install"
expect = "write"

[[tests]]
input = "docker build ."
expect = "write"

[[tests]]
input = "docker pull nginx"
expect = "write"

[[tests]]
input = "git commit -m 'test'"
expect = "write"

[[tests]]
input = "git checkout main"
expect = "write"

[[tests]]
input = "vim file.txt"
expect = "write"

## === Archive/extraction tools ===

[[tests]]
input = "tar -xvf file.tar"
expect = "write"
note = "-x extracts, which is a write"

[[tests]]
input = "tar -tf archive.tar"
expect = "safe"
note = "-t just lists contents"

[[tests]]
input = "unzip archive.zip"
expect = "write"
