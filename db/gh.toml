command = "gh"
default = "unknown"

[subcommands.repo]
default = "safe"

[subcommands.issue]
default = "safe"

[subcommands.pr]
default = "safe"

[subcommands.run]
default = "safe"

[subcommands.workflow]
default = "safe"

[subcommands.release]
default = "safe"

[subcommands.gist]
default = "safe"

[subcommands.search]
default = "safe"

[subcommands.browse]
default = "safe"

[subcommands.auth]
default = "safe"

[subcommands.api]
default = "safe"

[[subcommands.api.flags]]
flag = ["-X", "--method"]
effect = "unknown"
reason = "Depends on HTTP method"

[subcommands.api.flags.values]
GET = "safe"
POST = "write"
PUT = "write"
DELETE = "write"
PATCH = "write"
