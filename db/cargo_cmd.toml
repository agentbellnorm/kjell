command = "cargo"
default = "unknown"

[subcommands.build]
default = "safe"

[subcommands.test]
default = "safe"

[subcommands.run]
default = "unknown"

[subcommands.check]
default = "safe"

[subcommands.clippy]
default = "safe"

[subcommands.fmt]
default = "write"

[subcommands.version]
default = "safe"
